import 'package:flutter/material.dart';

/// Clean, readable palette — social-app friendly (not heavy sci-fi).
abstract final class AppColors {
  static const primary = Color(0xFF6366F1); // indigo
  static const quantumViolet = primary;
  static const secondary = Color(0xFF22D3EE); // cyan accent
  static const auroraCyan = secondary;
  static const accent = Color(0xFFF59E0B);
  static const plasmaGold = accent;
  static const background = Color(0xFF0F172A); // slate-900
  static const surface = Color(0xFF1E293B); // slate-800
  static const surfaceLight = Color(0xFF334155);
  static const glow = Color(0xFF818CF8);
  static const text = Color(0xFFF1F5F9);
  static const textMuted = Color(0xFF94A3B8);
  static const border = Color(0xFF475569);
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
}
